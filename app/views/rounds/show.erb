<h1> <%=@round.deck.name%> </h1>

<p>You've completed a round of <%=@round.deck.name%>. You answered <%=@round.guesses.where(correct:1).count%>/<%=@round.deck.cards.count%> cards correctly on the first try. You submitted <%=@round.guesses.count%> total guesses before each card was answered correctly.
